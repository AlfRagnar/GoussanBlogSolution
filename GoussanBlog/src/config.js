const config = {
  defaultEndpoint: "https://goussanblogdata.azurewebsites.net", // Set your default endpoint for axios to make API calls to
  // defaultEndpoint: "https://localhost:5001", // Set your default endpoint for axios to make API calls to
};

export default config;
